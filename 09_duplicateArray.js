

let arrN= [11, 22,33,22,11,55];
let duplicate=[]
for (let index = 0; index < arrN.length; index++) {
    let element = arrN[index];
    if(!duplicate.includes(element)) {
        //duplicate.push(element);
        //break;
        console.log(element);
    }
    //console.log(element);
    
}
//console.log(element);


